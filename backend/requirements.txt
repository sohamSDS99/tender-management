fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
alembic==1.14.0
pydantic==2.10.4
pydantic-settings==2.7.0
httpx==0.28.1
apscheduler==3.11.0
pyyaml==6.0.2
python-multipart==0.0.20
# PostgreSQL driver. SQLite needs no driver; this makes
# DATABASE_URL=postgresql+psycopg://... work out of the box.
psycopg[binary]==3.2.3
# Language identification for the Translate button. Pure-Python + numpy, no
# native build step, and the model file ships in the wheel - which matters on a
# platform where the image is rebuilt on every deploy. See D33 amended.
py3langid==0.3.0
